interface GlobalStyleProps {
  fontColor: string;
  bodyColor: string;
}

interface NavbarProps {
  title: string;
}

interface ButtonProps {
  text?: string;
  bgColor?: string;
  fontColor?: string;
  icon?: string;
}

interface ButtonStyles {
  bgColor?: string;
  fontColor?: string;
}
